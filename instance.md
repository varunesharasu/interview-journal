what is instance

An instance is an actual object created from a class.

Example:

class Student {
    String name;
}

Student s1 = new Student();

Here:

Student → class
s1 → object
s1 is also called an instance of Student class

Simple meaning:

Instance = object created from a class

So, object and instance are almost used with the same meaning in programming.
